# Задачник (2026)

Сборник задач для практик 1–2 курса Java.

Номера задач совпадают с таблицами вариантов в заданиях практик.

---

## 1

Известна сторона квадрата *a*. Найти его периметр *P* = 4·*a*.

## 2

Известна сторона квадрата *a*. Найти его площадь *S* = *a*<sup>2</sup>.

## 3

Известны стороны прямоугольника *a* и *b*. Найти его площадь *S* = *a*·*b* и периметр *P* = 2·(*a* + *b*).

## 4

Дан диаметр окружности *d*. Найти ее длину *L* = π·*d*

## 5

Известна длина ребра куба *a*. Найти объем куба *V* = *a*<sup>3</sup> и площадь его поверхности *S* = 6·*a*<sup>2</sup>.

## 6

Известны длины ребер *a*, *b*, *c* прямоугольного параллелепипеда. Найти его объем *V* = *a·b·c* и площадь поверхности *S* = 2·(*a·b* + *b·c* + *a·c*).

## 7

Найти длину окружности *L* и площадь круга *S* заданного радиуса *R*:

> *L* = 2·π·*R*, *S* = π·*R*<sup>2</sup>.

## 8

Известны два числа *a* и *b*. Найти их *среднее арифметическое*: (*a* + *b*)/2.

## 9

Известны два неотрицательных числа *a* и *b*. Найти их *среднее геометрическое*, т. е. квадратный корень из их произведения: (*a*·*b*)<sup>1/2</sup>.

## 10

Известны два ненулевых числа. Найти сумму, разность, произведение и частное их квадратов.

## 11

Известны два ненулевых числа. Найти сумму, разность, произведение и частное их модулей.

## 12

Известны катеты прямоугольного треугольника *a* и *b*. Найти его гипотенузу *c* и периметр *P*:

> *c* = (*a*<sup>2</sup> + *b*<sup>2</sup>)<sup>1/2</sup>, *P* = *a* + *b* + *c*.

## 13

Известны два круга с общим центром и радиусами *R*<sub>1</sub> и *R*<sub>2</sub> (*R*<sub>1</sub> > *R*<sub>2</sub>). Найти площади этих кругов *S*<sub>1</sub> и *S*<sub>2</sub>, а также площадь *S*<sub>3</sub> кольца, внешний радиус которого равен *R*<sub>1</sub>, а внутренний радиус равен *R*<sub>2</sub>:

> *S*<sub>1</sub> = π·(*R*<sub>1</sub>)<sup>2</sup>, *S*<sub>2</sub> = π·(*R*<sub>2</sub>)<sup>2</sup>, *S*<sub>3</sub> = *S*<sub>1</sub> − *S*<sub>2</sub>.

## 14

Известна длина *L* окружности. Найти ее радиус *R* и площадь *S* круга, ограниченного этой окружностью, учитывая, что *L* = 2·π·*R*, *S* = π·*R*<sup>2</sup>.

## 15

Известна площадь *S* круга. Найти его диаметр *D* и длину *L* окружности, ограничивающей этот круг, учитывая, что *L* = π·*D*, *S* = π·*D*<sup>2</sup>/4.

## 16

Найти расстояние между двумя точками с заданными координатами *x*<sub>1</sub> и *x*<sub>2</sub> на числовой оси: |*x*<sub>2</sub> − *x*<sub>1</sub>|.

## 17

Известны три точки *A*, *B*, *C* на числовой оси. Найти длины отрезков *AC* и *BC* и их сумму.

## 18

Известны три точки *A*, *B*, *C* на числовой оси. Точка *C* расположена между точками *A* и *B*. Найти произведение длин отрезков *AC* и *BC*.

## 19

Известны координаты двух противоположных вершин прямоугольника: (*x*<sub>1</sub>, *y*<sub>1</sub>), (*x*<sub>2</sub>, *y*<sub>2</sub>). Стороны прямоугольника параллельны осям координат. Найти периметр и площадь данного прямоугольника.

## 20

Найти расстояние между двумя точками с заданными координатами (*x*<sub>1</sub>, *y*<sub>1</sub>) и (*x*<sub>2</sub>, *y*<sub>2</sub>) на плоскости. Расстояние вычисляется по формуле

> ((*x*<sub>2</sub> − *x*<sub>1</sub>)<sup>2</sup> + (*y*<sub>2</sub> − *y*<sub>1</sub>)<sup>2</sup>)<sup>1/2</sup>.

## 21

Известны координаты трех вершин треугольника: (*x*<sub>1</sub>, *y*<sub>1</sub>), (*x*<sub>2</sub>, *y*<sub>2</sub>), (*x*<sub>3</sub>, *y*<sub>3</sub>). Найти его периметр и площадь, используя формулу для расстояния между двумя точками на плоскости (см. задание 20). Для нахождения площади треугольника со сторонами *a*, *b*, *c* использовать *формулу Герона*:

> *S* = (*p*·(*p* − *a*)·(*p* − *b*)·(*p* − *c*))<sup>1/2</sup>,

где *p* = (*a* + *b* + *c*)/2 — *полупериметр*.

## 22

Поменять местами содержимое переменных *A* и *B* и вывести новые значения *A* и *B*.

## 23

Известны переменные *A*, *B*, *C*. Изменить их значения, переместив содержимое *A* в *B*, *B* — в *C*, *C* — в *A*, и вывести новые значения переменных *A*, *B*, *C*.

## 24

Известны переменные *A*, *B*, *C*. Изменить их значения, переместив содержимое *A* в *C*, *C* — в *B*, *B* — в *A*, и вывести новые значения переменных *A*, *B*, *C*.

## 25

Найти значение функции *y* = 3*x*<sup>6</sup> − 6*x*<sup>2</sup> − 7 при данном значении *x*.

## 26

Найти значение функции *y* = 4(*x*−3)<sup>6</sup> − 7(*x*−3)<sup>3</sup> + 2 при данном значении *x*.

## 27

Известно число *A*. Вычислить *A*<sup>8</sup>, используя вспомогательную переменную и три операции умножения. Для этого последовательно находить *A*<sup>2</sup>, *A*<sup>4</sup>, *A*<sup>8</sup>. Вывести все найденные степени числа *A*.

## 28

Известно число *A*. Вычислить *A*<sup>15</sup>, используя две вспомогательные переменные и пять операций умножения. Для этого последовательно находить *A*<sup>2</sup>, *A*<sup>3</sup>, *A*<sup>5</sup>, *A*<sup>10</sup>, *A*<sup>15</sup>. Вывести все найденные степени числа *A*.

## 29

Известно значение угла α в градусах (0 ≤ α < 360). Определить значение этого же угла в радианах, учитывая, что 180° = π радианов.

## 30

Известно значение угла α в радианах (0 ≤ α < 2·π). Определить значение этого же угла в градусах, учитывая, что 180° = π радианов.

## 31

Известно значение температуры *T* в градусах Фаренгейта. Определить значение этой же температуры в градусах Цельсия. Температура по Цельсию *T*<sub>*C*</sub> и температура по Фаренгейту *T*<sub>*F*</sub> связаны следующим соотношением:

> *T*<sub>*C*</sub> = (*T*<sub>*F*</sub> − 32)·5/9.

## 32

Известно значение температуры *T* в градусах Цельсия. Определить значение этой же температуры в градусах Фаренгейта. Температура по Цельсию *T*<sub>*C*</sub> и температура по Фаренгейту *T*<sub>*F*</sub> связаны следующим соотношением:

> *T*<sub>*F*</sub> = *T*<sub>*C*</sub>·9/5 + 32.

## 33

Известно, что *X* кг конфет стоит *A* рублей. Определить, сколько стоит 1 кг и *Y* кг этих же конфет.

## 34

Известно, что *X* кг шоколадных конфет стоит *A* рублей, а *Y* кг ирисок стоит *B* рублей. Определить, сколько стоит 1 кг шоколадных конфет, 1 кг ирисок, а также во сколько раз шоколадные конфеты дороже ирисок.

## 35

Скорость лодки в стоячей воде *V* км/ч, скорость течения реки *U* км/ч (*U* < *V*). Время движения лодки по озеру *T*<sub>1</sub> ч, а по реке (против течения) — *T*<sub>2</sub> ч. Определить путь *S*, пройденный лодкой (путь = время · скорость). Учесть, что при движении против течения скорость лодки уменьшается на величину скорости течения.

## 36

Скорость первого автомобиля *V*<sub>1</sub> км/ч, второго — *V*<sub>2</sub> км/ч, расстояние между ними *S* км. Определить расстояние между ними через *T* часов, если автомобили удаляются друг от друга, двигаясь в противоположных направлениях. Данное расстояние равно сумме начального расстояния и общего пути, проделанного автомобилями; общий путь = время · суммарная скорость.

## 37

Скорость первого автомобиля *V*<sub>1</sub> км/ч, второго — *V*<sub>2</sub> км/ч, расстояние между ними *S* км. Определить расстояние между ними через *T* часов, если автомобили первоначально движутся навстречу друг другу. Данное расстояние равно модулю разности начального расстояния и общего пути, проделанного автомобилями; общий путь = время · суммарная скорость.

## 38

Решить линейное уравнение *A*·*x* + *B* = 0, заданное своими коэффициентами *A* и *B* (коэффициент *A* не равен 0).

## 39

Найти корни *квадратного уравнения* *A*·*x*<sup>2</sup> + *B*·*x* + *C* = 0, заданного своими коэффициентами *A*, *B*, *C* (*A* > 0), если известно, что дискриминант уравнения положителен. Вывести вначале меньший, а затем больший из найденных корней. Корни квадратного уравнения находятся по формуле

> *x*<sub>1, 2</sub> = (−*B* ± (*D*)<sup>1/2</sup>)/(2·*A*),

где *D* — *дискриминант*, равный *B*<sup>2</sup> − 4·*A*·*C*.

## 40

Найти решение *системы линейных уравнений* вида

> *A*<sub>1</sub>·*x* + *B*<sub>1</sub>·*y* = *C*<sub>1</sub>,
> *A*<sub>2</sub>·*x* + *B*<sub>2</sub>·*y* = *C*<sub>2</sub>,

заданной своими коэффициентами *A*<sub>1</sub>, *B*<sub>1</sub>, *C*<sub>1</sub>, *A*<sub>2</sub>, *B*<sub>2</sub>, *C*<sub>2</sub>, если известно, что данная система имеет единственное решение. Воспользоваться формулами

> *x* = (*C*<sub>1</sub>·*B*<sub>2</sub> − *C*<sub>2</sub>·*B*<sub>1</sub>)/*D*, *y* = (*A*<sub>1</sub>·*C*<sub>2</sub> − *A*<sub>2</sub>·*C*<sub>1</sub>)/*D*,
> где *D* = *A*<sub>1</sub>·*B*<sub>2</sub> − *A*<sub>2</sub>·*B*<sub>1</sub>.

## 41

Известно целое число. Если оно является положительным, то вычесть из него 8; в противном случае не изменять его. Вывести полученное число.

## 42

Известно целое число. Если оно является положительным, то вычесть из него 8; в противном случае прибавить к нему 6. Вывести полученное число.

## 43

Известно целое число. Если оно является положительным, то вычесть из него 8; если отрицательным, то прибавить к нему 6; если нулевым, то заменить его на 10. Вывести полученное число.

## 44

Известны три целых числа. Найти количество положительных чисел в исходном наборе.

## 45

Известны три целых числа. Найти количество положительных и количество отрицательных чисел в исходном наборе.

## 46

Известны два числа. Вывести большее из них.

## 47

Известны два числа. Вывести порядковый номер меньшего из них.

## 48

Известны два числа. Вывести вначале большее, а затем меньшее из них.

## 49

Известны две переменные вещественного типа: *A*, *B*. Перераспределить значения данных переменных так, чтобы в *A* оказалось меньшее из значений, а в *B* — большее. Вывести новые значения переменных *A* и *B*.

## 50

Известны две переменные целого типа: *A* и *B*. Если их значения не равны, то присвоить каждой переменной сумму этих значений, а если равны, то присвоить переменным нулевые значения. Вывести новые значения переменных *A* и *B*.

## 51

Известны две переменные целого типа: *A* и *B*. Если их значения не равны, то присвоить каждой переменной большее из этих значений, а если равны, то присвоить переменным нулевые значения. Вывести новые значения переменных *A* и *B*.

## 52

Известны три числа. Найти наименьшее из них.

## 53

Известны три числа. Найти среднее из них (т. е. число, расположенное между наименьшим и наибольшим).

## 54

Известны три числа. Вывести вначале наименьшее, а затем наибольшее из данных чисел.

## 55

Известны три числа. Найти сумму двух наибольших из них.

## 56

Известны три переменные вещественного типа: *A*, *B*, *C*. Если их значения упорядочены по возрастанию, то удвоить их; в противном случае заменить значение каждой переменной на противоположное. Вывести новые значения переменных *A*, *B*, *C*.

## 57

Известны три переменные вещественного типа: *A*, *B*, *C*. Если их значения упорядочены по возрастанию или убыванию, то удвоить их; в противном случае заменить значение каждой переменной на противоположное. Вывести новые значения переменных *A*, *B*, *C*.

## 58

Известны три целых числа, одно из которых отлично от двух других, равных между собой. Определить порядковый номер числа, отличного от остальных.

## 59

Известны четыре целых числа, одно из которых отлично от трех других, равных между собой. Определить порядковый номер числа, отличного от остальных.

## 60

На числовой оси расположены три точки: *A*, *B*, *C*. Определить, какая из двух последних точек (*B* или *C*) расположена ближе к *A*, и вывести эту точку и ее расстояние от точки *A*.

## 61

Известны целочисленные координаты точки на плоскости. Если точка совпадает с началом координат, то вывести 0. Если точка не совпадает с началом координат, но лежит на оси *OX* или *OY*, то вывести соответственно 1 или 2. Если точка не лежит на координатных осях, то вывести 3.

## 62

Известны координаты точки, не лежащей на координатных осях *OX* и *OY*. Определить номер координатной четверти, в которой находится данная точка.

## 63

Известны целочисленные координаты трех вершин прямоугольника, стороны которого параллельны координатным осям. Найти координаты его четвертой вершины.

## 64

Для данного вещественного *x* найти значение следующей функции *f*, принимающей вещественные значения:

> *f*(*x*) = 2·sin(*x*), если *x* > 0,
> 6 − *x*, если *x* ≤ 0.

## 65

Для данного целого *x* найти значение следующей функции *f*, принимающей значения целого типа:

> *f*(*x*) = 2·*x*, если *x* < −2 или *x* > 2,
> −3·*x* в противном случае.

## 66

Для данного вещественного *x* найти значение следующей функции *f*, принимающей вещественные значения:

> −*x*, если *x* ≤ 0,
> *f*(*x*) = *x*<sup>2</sup>, если 0 < *x* < 2,
> 4, если *x* ≥ 2.

## 67

Для данного вещественного *x* найти значение следующей функции *f*, принимающей значения целого типа:

> 0, если *x* < 0,
> *f*(*x*) = 1, если *x* принадлежит [0, 1), [2, 3), …,
> −1, если *x* принадлежит [1, 2), [3, 4), … .

## 68

Дан номер года (положительное целое число). Определить количество дней в этом году, учитывая, что обычный год насчитывает 365 дней, а високосный — 366 дней. Високосным считается год, делящийся на 4, за исключением тех годов, которые делятся на 100 и не делятся на 400 (например, годы 300, 1300 и 1900 не являются високосными, а 1200 и 2000 — являются).

## 69

Известно целое число. Вывести его строку-описание вида «отрицательное четное число», «нулевое число», «положительное нечетное число» и т. д.

## 70

Известно целое число, лежащее в диапазоне 1–999. Вывести его строку-описание вида «четное двузначное число», «нечетное трехзначное число» и т. д.

## 71

Известны целые числа *K* и *N* (*N* > 0). Вывести *N* раз число *K*.

## 72

Известны два целых числа *A* и *B* (*A* < *B*). Вывести в порядке возрастания все целые числа, расположенные между *A* и *B* (включая сами числа *A* и *B*), а также количество *N* этих чисел.

## 73

Известны два целых числа *A* и *B* (*A* < *B*). Вывести в порядке убывания все целые числа, расположенные между *A* и *B* (не включая числа *A* и *B*), а также количество *N* этих чисел.

## 74

Известно вещественное число — цена 1 кг конфет. Вывести стоимость 1, 2, …, 10 кг конфет.

## 75

Известно вещественное число — цена 1 кг конфет. Вывести стоимость 0.1, 0.2, …, 1 кг конфет.

## 76

Известно вещественное число — цена 1 кг конфет. Вывести стоимость 1.2, 1.4, …, 2 кг конфет.

## 77

Известны два целых числа *A* и *B* (*A* < *B*). Найти сумму всех целых чисел от *A* до *B* включительно.

## 78

Известны два целых числа *A* и *B* (*A* < *B*). Найти произведение всех целых чисел от *A* до *B* включительно.

## 79

Известны два целых числа *A* и *B* (*A* < *B*). Найти сумму кубов всех целых чисел от *A* до *B* включительно.

## 80

Известно целое число *N* (> 0). Найти сумму

> 1 + 1/2 + 1/3 + … + 1/*N*

(вещественное число).

## 81

Известно целое число *N* (> 0). Найти сумму

> *N*<sup>2</sup> + (*N* + 1)<sup>2</sup> + (*N* + 2)<sup>2</sup> + … + (2·*N*)<sup>2</sup>

(целое число).

## 82

Известно целое число *N* (> 0). Найти произведение

> 1.1 · 1.2 · 1.3 · …

(*N* сомножителей).

## 83

Известно целое число *N* (> 0). Найти значение выражения

> 1.1 − 1.2 + 1.3 − …

(*N* слагаемых, знаки чередуются). Условный оператор не использовать.

## 84

Известно целое число *N* (> 0). Найти квадрат данного числа, используя для его вычисления следующую формулу:

> *N*<sup>2</sup> = 1 + 3 + 5 + … + (2·*N* − 1).

После добавления к сумме каждого слагаемого выводить текущее значение суммы (в результате будут выведены квадраты всех целых чисел от 1 до *N*).

## 85

Известно вещественное число *A* и целое число *N* (> 0). Найти *A* в степени *N*:

> *A*<sup>*N*</sup> = *A*·*A*· … ·*A*

(числа *A* перемножаются *N* раз).

## 86

Известно вещественное число *A* и целое число *N* (> 0). Используя один цикл, вывести все целые степени числа *A* от 1 до *N*.

## 87

Известно вещественное число *A* и целое число *N* (> 0). Используя один цикл, найти сумму

> 1 + *A* + *A*<sup>2</sup> + *A*<sup>3</sup> + … + *A*<sup>*N*</sup>.

## 88

Известно вещественное число *A* и целое число *N* (> 0). Используя один цикл, найти значение выражения

> 1 − *A* + *A*<sup>2</sup> − *A*<sup>3</sup> + … + (−1)<sup>*N*</sup>·*A*<sup>*N*</sup>.

Условный оператор не использовать.

## 89

Известно целое число *N* (> 0). Найти произведение

> *N*! = 1·2·…·*N*

(**N*–факториал*). Чтобы избежать целочисленного переполнения, вычислять это произведение с помощью вещественной переменной и вывести его как вещественное число.

## 90

Известно целое число *N* (> 0). Используя один цикл, найти сумму

> 1! + 2! + 3! + … + *N*!

(выражение *N*! — **N*–факториал* — обозначает произведение всех целых чисел от 1 до *N*: *N*! = 1·2·…·*N*). Чтобы избежать целочисленного переполнения, проводить вычисления с помощью вещественных переменных и вывести результат как вещественное число.

## 91

Известно целое число *N* (> 0). Используя один цикл, найти сумму

> 1 + 1/(1!) + 1/(2!) + 1/(3!) + … + 1/(*N*!)

(выражение *N*! — **N*–факториал* — обозначает произведение всех целых чисел от 1 до *N*: *N*! = 1·2·…·*N*). Полученное число является приближенным значением константы *e* = exp(1) (проверить это).

## 92

Известно вещественное число *X* и целое число *N* (> 0). Найти значение выражения

> 1 + *X* + *X*<sup>2</sup>/(2!) + … + *X*<sup>*N*</sup>/(*N*!)

(*N*! = 1·2·…·*N*). Полученное число является приближенным значением функции exp в точке *X* (проверить это).

## 93

Известно вещественное число *X* и целое число *N* (> 0). Найти значение выражения

> *X* − *X*<sup>3</sup>/(3!) + *X*<sup>5</sup>/(5!) − … + (−1)<sup>*N*</sup>·*X*<sup>2·*N*+1</sup>/((2·*N*+1)!)

(*N*! = 1·2·…·*N*). Полученное число является приближенным значением функции sin в точке *X* (проверить это).

## 94

Известно вещественное число *X* и целое число *N* (> 0). Найти значение выражения

> 1 − *X*<sup>2</sup>/(2!) + *X*<sup>4</sup>/(4!) − … + (−1)<sup>*N*</sup>·*X*<sup>2·*N*</sup>/((2·*N*)!)

(*N*! = 1·2·…·*N*). Полученное число является приближенным значением функции cos в точке *X* (проверить это).

## 95

Известно вещественное число *X* (|*X*| < 1) и целое число *N* (> 0). Найти значение выражения

> *X* − *X*<sup>2</sup>/2 + *X*<sup>3</sup>/3 − … + (−1)<sup>*N*−1</sup>·*X*<sup>*N*</sup>/*N*.

Полученное число является приближенным значением функции ln в точке 1 + *X* (проверить это).

## 96

Известно вещественное число *X* (|*X*| < 1) и целое число *N* (> 0). Найти значение выражения

> *X* − *X*<sup>3</sup>/3 + *X*<sup>5</sup>/5 − … + (−1)<sup>*N*</sup>·*X*<sup>2·*N*+1</sup>/(2·*N*+1).

Полученное число является приближенным значением функции arctg в точке *X* (проверить это).

## 97

Известно вещественное число *X* (|*X*| < 1) и целое число *N* (> 0). Найти значение выражения

> *X* + 1·*X*<sup>3</sup>/(2·3) + 1·3·*X*<sup>5</sup>/(2·4·5) + … +
> + 1·3·…·(2·*N*−1)·*X*<sup>2·*N*+1</sup>/(2·4·…·(2·*N*)·(2·*N*+1)).

Полученное число является приближенным значением функции arcsin в точке *X* (проверить это).

## 98

Известно вещественное число *X* (|*X*| < 1) и целое число *N* (> 0). Найти значение выражения

> 1 + *X*/2 − 1·*X*<sup>2</sup>/(2·4) + 1·3·*X*<sup>3</sup>/(2·4·6) − … +
> + (−1)<sup>*N*−1</sup>·1·3·…·(2·*N*−3)·*X*<sup>*N*</sup>/(2·4·…·(2·*N*)).

Полученное число является приближенным значением функции (1+*X*)<sup>1/2</sup> (проверить это).

## 99

Известно целое число *N* (> 1) и две вещественные точки на числовой оси: *A*, *B* (*A* < *B*). Отрезок [*A*, *B*] разбит на *N* равных отрезков. Вывести *H* — длину каждого отрезка, а также набор точек

> *A*, *A* + *H*, *A* + 2·*H*, *A* + 3·*H*, …, *B*,

образующий разбиение отрезка [*A*, *B*].

## 100

Известно целое число *N* (> 1) и две вещественные точки на числовой оси: *A*, *B* (*A* < *B*). Отрезок [*A*, *B*] разбит на *N* равных отрезков. Вывести *H* — длину каждого отрезка, а также значения функции *F*(*X*) = 1 − sin(*X*) в точках, разбивающих отрезок [*A*, *B*]:

> *F*(*A*), *F*(*A* + *H*), *F*(*A* + 2·*H*), …, *F*(*B*).

## 101

Известно целое число *N* (> 0). Последовательность вещественных чисел *A*<sub>*K*</sub> определяется следующим образом:

> *A*<sub>0</sub> = 2, *A*<sub>*K*</sub> = 2 + 1/*A*<sub>*K*−1</sub>, *K* = 1, 2, … .

Вывести элементы *A*<sub>1</sub>, *A*<sub>2</sub>, …, *A*<sub>*N*</sub>.

## 102

Известно целое число *N* (> 0). Последовательность вещественных чисел *A*<sub>*K*</sub> определяется следующим образом:

> *A*<sub>0</sub> = 1, *A*<sub>*K*</sub> = (*A*<sub>*K*−1</sub> + 1)/*K*, *K* = 1, 2, … .

Вывести элементы *A*<sub>1</sub>, *A*<sub>2</sub>, …, *A*<sub>*N*</sub>.

## 103

Известно целое число *N* (> 1). Последовательность *чисел Фибоначчи* *F*<sub>*K*</sub> (целого типа) определяется следующим образом:

> *F*<sub>1</sub> = 1, *F*<sub>2</sub> = 1, *F*<sub>*K*</sub> = *F*<sub>*K*−2</sub> + *F*<sub>*K*−1</sub>, *K* = 3, 4, … .

Вывести элементы *F*<sub>1</sub>, *F*<sub>2</sub>, …, *F*<sub>*N*</sub>.

## 104

Известно целое число *N* (> 1). Последовательность вещественных чисел *A*<sub>*K*</sub> определяется следующим образом:

> *A*<sub>1</sub> = 1, *A*<sub>2</sub> = 2, *A*<sub>*K*</sub> = (*A*<sub>*K*−2</sub> + 2·*A*<sub>*K*−1</sub>)/3, *K* = 3, 4, … .

Вывести элементы *A*<sub>1</sub>, *A*<sub>2</sub>, …, *A*<sub>*N*</sub>.

## 105

Известно целое число *N* (> 2). Последовательность целых чисел *A*<sub>*K*</sub> определяется следующим образом:

> *A*<sub>1</sub> = 1, *A*<sub>2</sub> = 2, *A*<sub>3</sub> = 3, *A*<sub>*K*</sub> = *A*<sub>*K*−1</sub> + *A*<sub>*K*−2</sub> − 2·*A*<sub>*K*−3</sub>, *K* = 4, 5, … .

Вывести элементы *A*<sub>1</sub>, *A*<sub>2</sub>, …, *A*<sub>*N*</sub>.

## 106

Известны целые положительные числа *N* и *K*. Найти сумму

> 1<sup>*K*</sup> + 2<sup>*K*</sup> + … + *N*<sup>*K*</sup>.

Чтобы избежать целочисленного переполнения, вычислять слагаемые этой суммы с помощью вещественной переменной и выводить результат как вещественное число.

## 107

Известно целое число *N* (> 0). Найти сумму

> 1<sup>1</sup> + 2<sup>2</sup> + … + *N*<sup>*N*</sup>.

Чтобы избежать целочисленного переполнения, вычислять слагаемые этой суммы с помощью вещественной переменной и выводить результат как вещественное число.

## 108

Известно целое число *N* (> 0). Найти сумму

> 1<sup>*N*</sup> + 2<sup>*N*−1</sup> + … + *N*<sup>1</sup>.

Чтобы избежать целочисленного переполнения, вычислять слагаемые этой суммы с помощью вещественной переменной и выводить результат как вещественное число.

## 109

Известны целые положительные числа *A* и *B* (*A* < *B*). Вывести все целые числа от *A* до *B* включительно; при этом каждое число должно выводиться столько раз, каково его значение (например, число 3 выводится 3 раза).

## 110

Известны целые числа *A* и *B* (*A* < *B*). Вывести все целые числа от *A* до *B* включительно; при этом число *A* должно выводиться 1 раз, число *A* + 1 должно выводиться 2 раза и т. д.

## 111

Известны положительные числа *A* и *B* (*A* > *B*). На отрезке длины *A* размещено максимально возможное количество отрезков длины *B* (без наложений). Не используя операции умножения и деления, найти длину незанятой части отрезка *A*.

## 112

Известны положительные числа *A* и *B* (*A* > *B*). На отрезке длины *A* размещено максимально возможное количество отрезков длины *B* (без наложений). Не используя операции умножения и деления, найти количество отрезков *B*, размещенных на отрезке *A*.

## 113

Известны целые положительные числа *N* и *K*. Используя только операции сложения и вычитания, найти частное от деления нацело *N* на *K*, а также остаток от этого деления.

## 114

Известно целое число *N* (> 0). Если оно является степенью числа 3, то вывести true, если не является — вывести false.

## 115

Известно целое число *N* (> 0), являющееся некоторой степенью числа 2: *N* = 2<sup>*K*</sup>. Найти целое число *K* — показатель этой степени.

## 116

Известно целое число *N* (> 0). Найти *двойной факториал *N**:

> *N*!! = *N*·(*N*−2)·(*N*−4)·…

(последний сомножитель равен 2, если *N* — четное, и 1, если *N* — нечетное). Чтобы избежать целочисленного переполнения, вычислять это произведение с помощью вещественной переменной и вывести его как вещественное число.

## 117

Известно целое число *N* (> 0). Найти наименьшее целое положительное число *K*, квадрат которого превосходит *N*: *K*<sup>2</sup> > *N*. Функцию извлечения квадратного корня не использовать.

## 118

Известно целое число *N* (> 0). Найти наибольшее целое число *K*, квадрат которого не превосходит *N*: *K*<sup>2</sup> ≤ *N*. Функцию извлечения квадратного корня не использовать.

## 119

Известно целое число *N* (> 1). Найти наименьшее целое число *K*, при котором выполняется неравенство 3<sup>*K*</sup> > *N*.

## 120

Известно целое число *N* (> 1). Найти наибольшее целое число *K*, при котором выполняется неравенство 3<sup>*K*</sup> < *N*.

## 121

Известно целое число *N* (> 1). Вывести наименьшее из целых чисел *K*, для которых сумма 1 + 2 + … + *K* будет больше или равна *N*, и саму эту сумму.

## 122

Известно целое число *N* (> 1). Вывести наибольшее из целых чисел *K*, для которых сумма 1 + 2 + … + *K* будет меньше или равна *N*, и саму эту сумму.

## 123

Известно число *A* (> 1). Вывести наименьшее из целых чисел *K*, для которых сумма 1 + 1/2 + … + 1/*K* будет больше *A*, и саму эту сумму.

## 124

Известно число *A* (> 1). Вывести наибольшее из целых чисел *K*, для которых сумма 1 + 1/2 + … + 1/*K* будет меньше *A*, и саму эту сумму.

## 125

Начальный вклад в банке равен 1000 руб. В конце каждого месяца размер вклада увеличивается на *P* процентов от имеющейся суммы (*P* — вещественное число, 0 < *P* < 25). По данному *P* определить, через сколько месяцев размер вклада превысит 1100 руб., и вывести найденное количество месяцев *K* (целое число) и итоговый размер вклада *S* (вещественное число).

## 126

Спортсмен-лыжник начал тренировки, пробежав в первый день 10 км. Каждый следующий день он увеличивал длину пробега на *P* процентов от пробега предыдущего дня (*P* — вещественное, 0 < *P* < 50). По данному *P* определить, после какого дня суммарный пробег лыжника за все дни превысит 200 км, и вывести найденное количество дней *K* (целое) и суммарный пробег *S* (вещественное число).

## 127

Известно целое число *N* (> 0). Используя операции деления нацело и взятия остатка от деления, вывести все его цифры, начиная с самой правой (разряда единиц).

## 128

Известно целое число *N* (> 0). Используя операции деления нацело и взятия остатка от деления, найти количество и сумму его цифр.

## 129

Известно целое число *N* (> 0). Используя операции деления нацело и взятия остатка от деления, найти число, полученное при прочтении числа *N* справа налево.

## 130

Известно целое число *N* (> 0). С помощью операций деления нацело и взятия остатка от деления определить, имеется ли в записи числа *N* цифра «2». Если имеется, то вывести true, если нет — вывести false.

## 131

Известно целое число *N* (> 0). С помощью операций деления нацело и взятия остатка от деления определить, имеются ли в записи числа *N* нечетные цифры. Если имеются, то вывести true, если нет — вывести false.

## 132

Известно целое число *N* (> 1). Если оно является *простым*, т. е. не имеет положительных делителей, кроме 1 и самого себя, то вывести true, иначе вывести false.

## 133

Известны целые положительные числа *A* и *B*. Найти их *наибольший общий делитель* (НОД), используя *алгоритм Евклида*:

> НОД(*A*, *B*) = НОД(*B*, *A* mod *B*), если *B* ≠ 0; НОД(*A*, 0) = *A*,

где «mod» обозначает операцию взятия остатка от деления.

## 134

Известно целое число *N* (> 1). Последовательность *чисел Фибоначчи* *F*<sub>*K*</sub> определяется следующим образом:

> *F*<sub>1</sub> = 1, *F*<sub>2</sub> = 1, *F*<sub>*K*</sub> = *F*<sub>*K*−2</sub> + *F*<sub>*K*−1</sub>, *K* = 3, 4, … .

Проверить, является ли число *N* числом Фибоначчи. Если является, то вывести true, если нет — вывести false.

## 135

Известно целое число *N* (> 1). Найти первое число Фибоначчи, большее *N*

## 136

Известно целое число *N* (> 1), являющееся числом Фибоначчи: *N* = *F*<sub>*K*</sub> Найти целые числа *F*<sub>*K*−1</sub> и *F*<sub>*K*+1</sub> — предыдущее и последующее числа Фибоначчи.

## 137

Известно целое число *N* (> 1), являющееся числом Фибоначчи: *N* = *F*<sub>*K*</sub> Найти целое число *K* — порядковый номер числа Фибоначчи *N*.

## 138

Известно вещественное число ε (> 0). Последовательность вещественных чисел *A*<sub>*K*</sub> определяется следующим образом:

> *A*<sub>1</sub> = 2, *A*<sub>*K*</sub> = 2 + 1/*A*<sub>*K*−1</sub>, *K* = 2, 3, … .

Найти первый из номеров *K*, для которых выполняется условие |*A*<sub>*K*</sub> − *A*<sub>*K*−1</sub>| < ε, и вывести этот номер, а также числа *A*<sub>*K*−1</sub> и *A*<sub>*K*</sub>.

## 139

Известно вещественное число ε (> 0). Последовательность вещественных чисел *A*<sub>*K*</sub> определяется следующим образом:

> *A*<sub>1</sub> = 1, *A*<sub>2</sub> = 2, *A*<sub>*K*</sub> = (*A*<sub>*K*−2</sub> + 2·*A*<sub>*K*−1</sub>)/3, *K* = 3, 4, … .

Найти первый из номеров *K*, для которых выполняется условие |*A*<sub>*K*</sub> − *A*<sub>*K*−1</sub>| < ε, и вывести этот номер, а также числа *A*<sub>*K*−1</sub> и *A*<sub>*K*</sub>.

## 140

Известны положительные числа *A*, *B*, *C*. На прямоугольнике размера *A* × *B* размещено максимально возможное количество квадратов со стороной *C* (без наложений). Найти количество квадратов, размещенных на прямоугольнике. Операции умножения и деления не использовать.

## 141

Известно целое число *N* и набор из *N* чисел. Найти минимальный и максимальный из элементов данного набора и вывести их в указанном порядке.

## 142

Известно целое число *N* и набор из *N* прямоугольников, заданных своими сторонами — парами чисел (*a*, *b*). Найти минимальную площадь прямоугольника из данного набора.

## 143

Известно целое число *N* и набор из *N* прямоугольников, заданных своими сторонами — парами чисел (*a*, *b*). Найти максимальный периметр прямоугольника из данного набора.

## 144

Известно целое число *N* и набор из *N* чисел. Найти номер минимального элемента из данного набора.

## 145

Известно целое число *N* и набор из *N* пар чисел (*m*, *v*) — данные о массе *m* и объеме *v* деталей, изготовленных из различных материалов. Вывести номер детали, изготовленной из материала максимальной плотности, а также величину этой максимальной плотности. Плотность *P* вычисляется по формуле

> *P* = *m*/*v*.

## 146

Известно целое число *N* и набор из *N* целых чисел. Найти номера первого минимального и последнего максимального элемента из данного набора и вывести их в указанном порядке.

## 147

Известно целое число *N* и набор из *N* целых чисел. Найти номера первого максимального и последнего минимального элемента из данного набора и вывести их в указанном порядке.

## 148

Известно целое число *N* и набор из *N* целых чисел. Найти номера первого и последнего минимального элемента из данного набора и вывести их в указанном порядке.

## 149

Известно целое число *N* и набор из *N* целых чисел. Найти номера первого и последнего максимального элемента из данного набора и вывести их в указанном порядке.

## 150

Известно целое число *N* и набор из *N* целых чисел. Найти номер первого *экстремального* (т. е. минимального или максимального) элемента из данного набора.

## 151

Известно целое число *N* и набор из *N* целых чисел. Найти номер последнего *экстремального* (т. е. минимального или максимального) элемента из данного набора.

## 152

Известно целое число *N* и набор из *N* чисел. Найти минимальное положительное число из данного набора. Если положительные числа в наборе отсутствуют, то вывести 0.

## 153

Известно целое число *N* и набор из *N* целых чисел. Найти номер первого максимального нечетного числа из данного набора. Если нечетные числа в наборе отсутствуют, то вывести 0.

## 154

Известно число *B* (> 0) и набор из десяти чисел. Вывести минимальный из тех элементов набора, которые больше *B*, а также его номер. Если чисел, больших *B*, в наборе нет, то дважды вывести 0.

## 155

Известны числа *B*, *C* (0 < *B* < *C*) и набор из десяти чисел. Вывести максимальный из элементов набора, содержащихся в интервале (*B*, *C*), и его номер. Если требуемые числа в наборе отсутствуют, то дважды вывести 0.

## 156

Известно целое число *N* и набор из *N* целых чисел. Найти количество элементов, расположенных перед первым минимальным элементом.

## 157

Известно целое число *N* и набор из *N* целых чисел. Найти количество элементов, расположенных после последнего максимального элемента.

## 158

Известно целое число *N* и набор из *N* целых чисел. Найти количество элементов, содержащихся между первым и последним максимальным элементом. Если в наборе имеется единственный максимальный элемент, то вывести 0.

## 159

Известно целое число *N* и набор из *N* целых чисел. Найти количество минимальных элементов из данного набора.

## 160

Известно целое число *N* и набор из *N* целых чисел. Найти общее количество *экстремальных* (т. е. минимальных и максимальных) элементов из данного набора.

## 161

Известно целое число *N* (> 2) и набор из *N* чисел — значений некоторой величины, полученных в *N* опытах. Найти среднее значение этой величины. При вычислении среднего значения не учитывать минимальное и максимальное из имеющихся в наборе значений.

## 162

Известно целое число *N* (> 2) и набор из *N* чисел. Найти три наименьших элемента из данного набора и вывести эти элементы в порядке возрастания их значений.

## 163

Известно целое число *N* (> 3) и набор из *N* чисел. Найти три наибольших элемента из данного набора и вывести эти элементы в порядке убывания их значений.

## 164

Известно целое число *N* (> 1) и набор из *N* чисел. Найти максимальную сумму двух соседних чисел из данного набора.

## 165

Известно целое число *N* (> 1) и набор из *N* чисел. Найти номера двух соседних чисел из данного набора, произведение которых является минимальным, и вывести вначале меньший, а затем больший номер.

## 166

Известно целое число *N* и набор из *N* целых чисел. Найти максимальное количество четных чисел в наборе, идущих подряд. Если четные числа в наборе отсутствуют, то вывести 0.

## 167

Известно целое число *N* и набор из *N* целых чисел, содержащий только нули и единицы. Найти номер элемента, с которого начинается самая длинная последовательность одинаковых чисел, и количество элементов в этой последовательности. Если таких последовательностей несколько, то вывести номер первой из них.

## 168

Известно целое число *N* и набор из *N* целых чисел, содержащий только нули и единицы. Найти номер элемента, с которого начинается самая длинная последовательность единиц, и количество элементов в этой последовательности. Если таких последовательностей несколько, то вывести номер последней из них. Если единицы в исходном наборе отсутствуют, то дважды вывести 0.

## 169

Известно целое число *N* и набор из *N* целых чисел. Найти максимальное количество подряд идущих минимальных элементов из данного набора.

## 170

Известно целое число *N* и набор из *N* целых чисел. Найти минимальное количество подряд идущих максимальных элементов из данного набора.

## 171

Известно целое число *N* (> 0). Сформировать и вывести целочисленный массив размера *N*, содержащий *N* первых положительных нечетных чисел: 1, 3, 5, … .

## 172

Известно целое число *N* (> 0). Сформировать и вывести целочисленный массив размера *N*, содержащий степени двойки от первой до *N*-й: 2, 4, 8, 16, … .

## 173

Известно целое число *N* (> 1), а также первый член *A* и разность *D* *арифметической прогрессии*. Сформировать и вывести массив размера *N*, содержащий *N* первых членов данной прогрессии:

> *A*, *A* + *D*, *A* + 2·*D*, *A* + 3·*D*, … .

## 174

Известно целое число *N* (> 1), а также первый член *A* и знаменатель *D* *геометрической прогрессии*. Сформировать и вывести массив размера *N*, содержащий *N* первых членов данной прогрессии:

> *A*, *A*·*D*, *A*·*D*<sup>2</sup>, *A*·*D*<sup>3</sup>, … .

## 175

Известно целое число *N* (> 2). Сформировать и вывести целочисленный массив размера *N*, содержащий *N* первых элементов последовательности *чисел Фибоначчи* *F*<sub>*K*</sub>:

> *F*<sub>1</sub> = 1, *F*<sub>2</sub> = 1, *F*<sub>*K*</sub> = *F*<sub>*K*−2</sub> + *F*<sub>*K*−1</sub>, *K* = 3, 4, … .

## 176

Известны целые числа *N* (> 2), *A* и *B*. Сформировать и вывести целочисленный массив размера *N*, первый элемент которого равен *A*, второй равен *B*, а каждый последующий элемент равен сумме всех предыдущих.

## 177

Пользователь вводит массив размера *N*. Вывести его элементы в обратном порядке.

## 178

Дан целочисленный массив размера *N*. Вывести все содержащиеся в данном массиве нечетные числа в порядке возрастания их индексов, а также их количество *K*.

## 179

Дан целочисленный массив размера *N*. Вывести все содержащиеся в данном массиве четные числа в порядке убывания их индексов, а также их количество *K*.

## 180

Дан целочисленный массив размера *N*. Вывести вначале все содержащиеся в данном массиве четные числа в порядке возрастания их индексов, а затем — все нечетные числа в порядке убывания их индексов.

## 181

Пользователь вводит массив *A* размера *N* и целое число *K* (1 ≤ *K* ≤ *N*). Вывести элементы массива с порядковыми номерами, кратными *K*: *A*<sub>*K*</sub>, *A*<sub>2·*K*</sub>, *A*<sub>3·*K*</sub>, … . Условный оператор не использовать.

## 182

Пользователь вводит массив *A* размера *N* (*N* — четное число). Вывести его элементы с четными номерами в порядке возрастания номеров: *A*<sub>2</sub>, *A*<sub>4</sub>, *A*<sub>6</sub>, …, *A*<sub>*N*</sub>. Условный оператор не использовать.

## 183

Пользователь вводит массив *A* размера *N* (*N* — нечетное число). Вывести его элементы с нечетными номерами в порядке убывания номеров: *A*<sub>*N*</sub>, *A*<sub>*N*−2</sub>, *A*<sub>*N*−4</sub>, …, *A*<sub>1</sub>. Условный оператор не использовать.

## 184

Пользователь вводит массив *A* размера *N*. Вывести вначале его элементы с четными номерами (в порядке возрастания номеров), а затем — элементы с нечетными номерами (также в порядке возрастания номеров):

> *A*<sub>2</sub>, *A*<sub>4</sub>, *A*<sub>6</sub>, …, *A*<sub>1</sub>, *A*<sub>3</sub>, *A*<sub>5</sub>, … .

Условный оператор не использовать.

## 185

Пользователь вводит массив *A* размера *N*. Вывести вначале его элементы с нечетными номерами в порядке возрастания номеров, а затем — элементы с четными номерами в порядке убывания номеров:

> *A*<sub>1</sub>, *A*<sub>3</sub>, *A*<sub>5</sub>, …, *A*<sub>6</sub>, *A*<sub>4</sub>, *A*<sub>2</sub>.

Условный оператор не использовать.

## 186

Пользователь вводит массив *A* размера *N*. Вывести его элементы в следующем порядке:

> *A*<sub>1</sub>, *A*<sub>*N*</sub>, *A*<sub>2</sub>, *A*<sub>*N*−1</sub>, *A*<sub>3</sub>, *A*<sub>*N*−2</sub>, … .

## 187

Пользователь вводит массив *A* размера *N*. Вывести его элементы в следующем порядке:

> *A*<sub>1</sub>, *A*<sub>2</sub>, *A*<sub>*N*</sub>, *A*<sub>*N*−1</sub>, *A*<sub>3</sub>, *A*<sub>4</sub>, *A*<sub>*N*−2</sub>, *A*<sub>*N*−3</sub>, … .

## 188

Пользователь вводит массив *A* ненулевых целых чисел размера 10. Вывести значение первого из тех его элементов *A*<sub>*K*</sub>, которые удовлетворяют неравенству *A*<sub>*K*</sub> < *A*<sub>10</sub>. Если таких элементов нет, то вывести 0.

## 189

Дан целочисленный массив *A* размера 10. Вывести порядковый номер последнего из тех его элементов *A*<sub>*K*</sub>, которые удовлетворяют двойному неравенству *A*<sub>1</sub> < *A*<sub>*K*</sub> < *A*<sub>10</sub>. Если таких элементов нет, то вывести 0.

## 190

Пользователь вводит массив размера *N* и целые числа *K* и *L* (1 ≤ *K* ≤ *L* ≤ *N*). Найти сумму элементов массива с номерами от *K* до *L* включительно.

## 191

Пользователь вводит массив размера *N* и целые числа *K* и *L* (1 ≤ *K* ≤ *L* ≤ *N*). Найти среднее арифметическое элементов массива с номерами от *K* до *L* включительно.

## 192

Пользователь вводит массив размера *N* и целые числа *K* и *L* (1 < *K* ≤ *L* ≤ *N*). Найти сумму всех элементов массива, кроме элементов с номерами от *K* до *L* включительно.

## 193

Пользователь вводит массив размера *N* и целые числа *K* и *L* (1 < *K* ≤ *L* ≤ *N*). Найти среднее арифметическое всех элементов массива, кроме элементов с номерами от *K* до *L* включительно.

## 194

Дан целочисленный массив размера *N*, не содержащий одинаковых чисел. Проверить, образуют ли его элементы *арифметическую прогрессию* Если образуют, то вывести разность прогрессии, если нет — вывести 0.

## 195

Пользователь вводит массив ненулевых целых чисел размера *N*. Проверить, образуют ли его элементы *геометрическую прогрессию* с целочисленным знаменателем. Если образуют, то вывести знаменатель прогрессии, если нет — вывести 0.

## 196

Дан целочисленный массив размера *N*. Проверить, чередуются ли в нем четные и нечетные числа. Если чередуются, то вывести "ok", если нет, то вывести порядковый номер первого элемента, нарушающего закономерность.

## 197

Пользователь вводит массив ненулевых целых чисел размера *N*. Проверить, чередуются ли в нем положительные и отрицательные числа. Если чередуются, то вывести 0, если нет, то вывести порядковый номер первого элемента, нарушающего закономерность.

## 198

Пользователь вводит массив *A* размера *N*. Найти минимальный элемент из его элементов с четными номерами: *A*<sub>2</sub>, *A*<sub>4</sub>, *A*<sub>6</sub>, … .

## 199

Пользователь вводит массив *A* размера *N*. Найти максимальный элемент из его элементов с нечетными номерами: *A*<sub>1</sub>, *A*<sub>3</sub>, *A*<sub>5</sub>, … .

## 200

Пользователь вводит массив размера *N*. Найти номера тех элементов массива, которые больше своего правого соседа, и количество таких элементов. Найденные номера выводить в порядке их возрастания.

## 201

Пользователь вводит массив размера *N*. Найти номера тех элементов массива, которые больше своего левого соседа, и количество таких элементов. Найденные номера выводить в порядке их убывания.

## 202

Пользователь вводит массив размера *N*. Найти номер его первого локального минимума (*локальный минимум* — это элемент, который меньше любого из своих соседей).

## 203

Пользователь вводит массив размера *N*. Найти номер его последнего локального максимума (*локальный максимум* — это элемент, который больше любого из своих соседей).

## 204

Пользователь вводит массив размера *N*. Найти максимальный из его локальных минимумов (*локальный минимум* — это элемент, который меньше любого из своих соседей).

## 205

Пользователь вводит массив размера *N*. Найти минимальный из его локальных максимумов (*локальный максимум* — это элемент, который больше любого из своих соседей)

## 206

Пользователь вводит массив размера *N*. Найти максимальный из его элементов, не являющихся ни локальным минимумом, ни локальным максимумом (определения *локального минимума* и *локального максимума* известны в заданиях выше). Если таких элементов в массиве нет, то вывести 0 (как вещественное число).

## 207

Пользователь вводит массив размера *N*. Найти количество участков, на которых его элементы возрастают.

## 208

Пользователь вводит массив размера *N*. Найти количество участков, на которых его элементы убывают.

## 209

Пользователь вводит массив размера *N*. Найти количество его *промежутков монотонности* (т. е. участков, на которых его элементы возрастают или убывают).

## 210

Известно число *R* и массив *A* размера *N*. Найти элемент массива, который *наиболее близок* к числу *R* (т. е. такой элемент *A*<sub>*K*</sub>, для которого величина |*A*<sub>*K*</sub> − *R*| является минимальной).

## 211

Пользователь вводит массив размера *N*. Найти два соседних элемента, сумма которых максимальна, и вывести эти элементы в порядке возрастания их индексов.

## 212

Известно число *R* и массив размера *N*. Найти два соседних элемента массива, сумма которых наиболее близка к числу *R*, и вывести эти элементы в порядке возрастания их индексов (определение наиболее близких чисел дано в задании 210).

## 213

Дан целочисленный массив размера *N*, все элементы которого упорядочены (по возрастанию или по убыванию). Найти количество различных элементов в данном массиве.

## 214

Дан целочисленный массив размера *N*, содержащий ровно два одинаковых элемента. Найти номера одинаковых элементов и вывести эти номера в порядке возрастания.

## 215

Пользователь вводит массив размера *N*. Найти номера двух ближайших элементов из этого массива (т. е. элементов с наименьшим модулем разности) и вывести эти номера в порядке возрастания.

## 216

Известно число *R* и массив размера *N*. Найти два различных элемента массива, сумма которых наиболее близка к числу *R*, и вывести эти элементы в порядке возрастания их индексов (определение наиболее близких чисел дано в задании 210).

## 217

Дан целочисленный массив размера *N*. Найти количество различных элементов в данном массиве.

## 218

Дан целочисленный массив размера *N*. Найти максимальное количество его одинаковых элементов.

## 219

Дан целочисленный массив размера *N*. Если он является *перестановкой*, т. е. содержит все числа от 1 до *N*, то вывести 0; в противном случае вывести номер первого недопустимого элемента.

## 220

Дан целочисленный массив *A* размера *N*, являющийся перестановкой (определение *перестановки* дано в задании 219). Найти количество *инверсий* в данной перестановке, т. е. таких пар элементов *A*<sub>*I*</sub> и *A*<sub>*J*</sub>, в которых большее число находится слева от меньшего: *A*<sub>*I*</sub> > *A*<sub>*J*</sub> при *I* < *J*.

## 221

Известны массивы *A* и *B* одинакового размера *N*. Поменять местами их содержимое и вывести вначале элементы преобразованного массива *A*, а затем — элементы преобразованного массива *B*.

## 222

Пользователь вводит массив *A* размера *N*. Сформировать новый массив *B* того же размера, элементы которого определяются следующим образом:

> *B*<sub>*K*</sub> = 2·*A*<sub>*K*</sub>, если *A*<sub>*K*</sub> < 5,
> *A*<sub>*K*</sub>/2 в противном случае.

## 223

Известны два массива *A* и *B* одинакового размера *N*. Сформировать новый массив *C* того же размера, каждый элемент которого равен максимальному из элементов массивов *A* и *B* с тем же индексом.

## 224

Дан целочисленный массив *A* размера *N*. Переписать в новый целочисленный массив *B* все четные числа из исходного массива (в том же порядке) и вывести размер полученного массива *B* и его содержимое.

## 225

Дан целочисленный массив *A* размера *N* (≤ 15). Переписать в новый целочисленный массив *B* все элементы с нечетными порядковыми номерами (1, 3, …) и вывести размер полученного массива *B* и его содержимое. Условный оператор не использовать.

## 226

Дан целочисленный массив *A* размера *N* (≤ 15). Переписать в новый целочисленный массив *B* все элементы с порядковыми номерами, кратными трем (3, 6, …), и вывести размер полученного массива *B* и его содержимое. Условный оператор не использовать.

## 227

Дан целочисленный массив *A* размера *N*. Переписать в новый целочисленный массив *B* того же размера вначале все элементы исходного массива с четными номерами, а затем — с нечетными:

> *A*<sub>2</sub>, *A*<sub>4</sub>, *A*<sub>6</sub>, …, *A*<sub>1</sub>, *A*<sub>3</sub>, *A*<sub>5</sub>, … .

Условный оператор не использовать.

## 228

Пользователь вводит массив *A* размера *N*. Сформировать новый массив *B* того же размера по следующему правилу: элемент *B*<sub>*K*</sub> равен сумме элементов массива *A* с номерами от 1 до *K*.

## 229

Пользователь вводит массив *A* размера *N*. Сформировать новый массив *B* того же размера по следующему правилу: элемент *B*<sub>*K*</sub> равен среднему арифметическому элементов массива *A* с номерами от 1 до *K*.

## 230

Пользователь вводит массив *A* размера *N*. Сформировать новый массив *B* того же размера по следующему правилу: элемент *B*<sub>*K*</sub> равен сумме элементов массива *A* с номерами от *K* до *N*.

## 231

Пользователь вводит массив *A* размера *N*. Сформировать новый массив *B* того же размера по следующему правилу: элемент *B*<sub>*K*</sub> равен среднему арифметическому элементов массива *A* с номерами от *K* до *N*.

## 232

Пользователь вводит массив *A* размера *N*. Сформировать два новых массива *B* и *C*: в массив *B* записать все положительные элементы массива *A*, в массив *C* — все отрицательные (сохраняя исходный порядок следования элементов). Вывести вначале размер и содержимое массива *B*, а затем — размер и содержимое массива *C*.

## 233

Известны два массива *A* и *B* размера 5, элементы которых упорядочены по возрастанию. Объединить эти массивы так, чтобы результирующий массив *C* (размера 10) остался упорядоченным по возрастанию.

## 234

Известны три целочисленных массива *A*, *B* и *C* размера *N*<sub>*A*</sub>, *N*<sub>*B*</sub>, *N*<sub>*C*</sub> соответственно, элементы которых упорядочены по убыванию. Объединить эти массивы так, чтобы результирующий целочисленный массив *D* (размера *N*<sub>*A*</sub> + *N*<sub>*B*</sub> + *N*<sub>*C*</sub>) остался упорядоченным по убыванию.

## 235

Пользователь вводит массив *A* размера *N* и целое число *K* (1 ≤ *K* ≤ *N*). Преобразовать массив, увеличив каждый его элемент на исходное значение элемента *A*<sub>*K*</sub>.

## 236

Дан целочисленный массив размера *N*. Увеличить все четные числа, содержащиеся в массиве, на исходное значение первого четного числа. Если четные числа в массиве отсутствуют, то оставить массив без изменений.

## 237

Дан целочисленный массив размера *N*. Увеличить все нечетные числа, содержащиеся в массиве, на исходное значение последнего нечетного числа. Если нечетные числа в массиве отсутствуют, то оставить массив без изменений.

## 238

Пользователь вводит массив размера *N*. Поменять местами его минимальный и максимальный элементы.

## 239

Пользователь вводит массив размера *N* (*N* — четное число). Поменять местами его первый элемент со вторым, третий — с четвертым и т. д.

## 240

Пользователь вводит массив размера *N* (*N* — четное число). Поменять местами первую и вторую половины массива.

## 241

Пользователь вводит массив размера *N*. Поменять порядок его элементов на обратный.

## 242

Пользователь вводит массив *A* размера *N* и целые числа *K* и *L* (1 ≤ *K* < *L* ≤ *N*). Переставить в обратном порядке элементы массива, расположенные между элементами *A*<sub>*K*</sub> и *A*<sub>*L*</sub>, включая эти элементы.

## 243

Пользователь вводит массив *A* размера *N* и целые числа *K* и *L* (1 ≤ *K* < *L* ≤ *N*). Переставить в обратном порядке элементы массива, расположенные между элементами *A*<sub>*K*</sub> и *A*<sub>*L*</sub>, не включая эти элементы.

## 244

Пользователь вводит массив размера *N*. Обнулить элементы массива, расположенные между его минимальным и максимальным элементами (не включая минимальный и максимальный элементы).

## 245

Пользователь вводит массив размера *N*. Переставить в обратном порядке элементы массива, расположенные между его минимальным и максимальным элементами, включая минимальный и максимальный элементы.

## 246

Пользователь вводит массив размера *N*. Обнулить все его *локальные* *максимумы* (т. е. числа, большие своих соседей).

## 247

Пользователь вводит массив размера *N*. Возвести в квадрат все его *локальные* *минимумы* (т. е. числа, меньшие своих соседей).

## 248

Пользователь вводит массив размера *N*. Заменить каждый элемент массива на среднее арифметическое этого элемента и его соседей.

## 249

Пользователь вводит массив размера *N*. Осуществить *сдвиг* элементов массива вправо на одну позицию (при этом *A*<sub>1</sub> перейдет в *A*<sub>2</sub>, *A*<sub>2</sub> — в *A*<sub>3</sub>, …, *A*<sub>*N*−1</sub> — в *A*<sub>*N*</sub>, a исходное значение последнего элемента будет потеряно). Первый элемент полученного массива положить равным 0.

## 250

Пользователь вводит массив размера *N*. Осуществить *сдвиг* элементов массива влево на одну позицию (при этом *A*<sub>*N*</sub> перейдет в *A*<sub>*N*−1</sub>, *A*<sub>*N*−1</sub> — в *A*<sub>*N*−2</sub>, …, *A*<sub>2</sub> — в *A*<sub>1</sub>, a исходное значение первого элемента будет потеряно). Последний элемент полученного массива положить равным 0.

## 251

Пользователь вводит массив размера *N* и целое число *K* (1 ≤ *K* < *N*). Осуществить *сдвиг* элементов массива вправо на *K* позиций (при этом *A*<sub>1</sub> перейдет в *A*<sub>*K*+1</sub>, *A*<sub>2</sub> — в *A*<sub>*K*+2</sub>, …, *A*<sub>*N*−*K*</sub> — в *A*<sub>*N*</sub>, а исходное значение *K* последних элементов будет потеряно). Первые *K* элементов полученного массива положить равными 0.

## 252

Пользователь вводит массив размера *N* и целое число *K* (1 ≤ *K* < *N*). Осуществить *сдвиг* элементов массива влево на *K* позиций (при этом *A*<sub>*N*</sub> перейдет в *A*<sub>*N*−*K*</sub>, *A*<sub>*N*−1</sub> — в *A*<sub>*N*−*K*−1</sub>, …, *A*<sub>*K*+1</sub> — в *A*<sub>1</sub>, а исходное значение *K* первых элементов будет потеряно). Последние *K* элементов полученного массива положить равными 0.

## 253

Пользователь вводит массив размера *N*. Осуществить *циклический сдвиг* элементов массива вправо на одну позицию (при этом *A*<sub>1</sub> перейдет в *A*<sub>2</sub>, *A*<sub>2</sub> — в *A*<sub>3</sub>, …, *A*<sub>*N*</sub> — в *A*<sub>1</sub>).

## 254

Пользователь вводит массив размера *N*. Осуществить *циклический сдвиг* элементов массива влево на одну позицию (при этом *A*<sub>*N*</sub> перейдет в *A*<sub>*N*−1</sub>, *A*<sub>*N*−1</sub> — в *A*<sub>*N*−2</sub>, …, *A*<sub>1</sub> — в *A*<sub>*N*</sub>).

## 255

Пользователь вводит массив *A* размера *N* и целое число *K* (1 ≤ *K* ≤ 4, *K* < *N*). Осуществить *циклический сдвиг* элементов массива вправо на *K* позиций (при этом *A*<sub>1</sub> перейдет в *A*<sub>*K*+1</sub>, *A*<sub>2</sub> — в *A*<sub>*K*+2</sub>, …, *A*<sub>*N*</sub> — в *A*<sub>*K*</sub>). Допускается использовать вспомогательный массив из 4 элементов.

## 256

Пользователь вводит массив *A* размера *N* и целое число *K* (1 ≤ *K* ≤ 4, *K* < *N*). Осуществить *циклический сдвиг* элементов массива влево на *K* позиций (при этом *A*<sub>*N*</sub> перейдет в *A*<sub>*N*−*K*</sub>, *A*<sub>*N*−1</sub> — в *A*<sub>*N*−*K*−1</sub>, …, *A*<sub>1</sub> — в *A*<sub>*N*−*K*+1</sub>). Допускается использовать вспомогательный массив из 4 элементов.

## 257

Пользователь вводит массив размера *N*, все элементы которого, кроме первого, упорядочены по возрастанию. Сделать массив упорядоченным, переместив первый элемент на новую позицию.

## 258

Пользователь вводит массив размера *N*, все элементы которого, кроме последнего, упорядочены по возрастанию. Сделать массив упорядоченным, переместив последний элемент на новую позицию.

## 259

Пользователь вводит массив размера *N*, все элементы которого, кроме одного, упорядочены по убыванию. Сделать массив упорядоченным, переместив элемент, нарушающий упорядоченность, на новую позицию.

## 260

Пользователь вводит массив размера *N* и целое число *K* (1 ≤ *K* ≤ *N*). Удалить из массива элемент с порядковым номером *K*.

## 261

Пользователь вводит массив размера *N* и целые числа *K* и *L* (1 ≤ *K* < *L* ≤ *N*). Удалить из массива элементы с номерами от *K* до *L* включительно и вывести размер полученного массива и его содержимое.

## 262

Дан целочисленный массив размера *N*. Удалить из массива все нечетные числа и вывести размер полученного массива и его содержимое.

## 263

Дан целочисленный массив размера *N* (> 2). Удалить из массива все элементы с четными номерами (2, 4, …). Условный оператор не использовать.

## 264

Дан целочисленный массив размера *N* (> 2). Удалить из массива все элементы с нечетными номерами (1, 3, …). Условный оператор не использовать.

## 265

Дан целочисленный массив размера *N*. Удалить из массива все соседние одинаковые элементы, оставив их первые вхождения.

## 266

Дан целочисленный массив размера *N*. Удалить из массива все одинаковые элементы, оставив их первые вхождения.

## 267

Дан целочисленный массив размера *N*. Удалить из массива все одинаковые элементы, оставив их последние вхождения.

## 268

Дан целочисленный массив размера *N*. Удалить из массива все элементы, встречающиеся менее трех раз, и вывести размер полученного массива и его содержимое.

## 269

Дан целочисленный массив размера *N*. Удалить из массива все элементы, встречающиеся более двух раз, и вывести размер полученного массива и его содержимое.

## 270

Дан целочисленный массив размера *N*. Удалить из массива все элементы, встречающиеся ровно два раза, и вывести размер полученного массива и его содержимое.

## 271

Пользователь вводит массив размера *N* и целое число *K* (1 ≤ *K* ≤ *N*). Перед элементом массива с порядковым номером *K* вставить новый элемент с нулевым значением.

## 272

Пользователь вводит массив размера *N* и целое число *K* (1 ≤ *K* ≤ *N*). После элемента массива с порядковым номером *K* вставить новый элемент с нулевым значением.

## 273

Пользователь вводит массив размера *N*. Вставить элемент с нулевым значением перед минимальным и после максимального элемента массива.

## 274

Пользователь вводит массив размера *N* и два целых числа *K* и *M* (1 ≤ *K* ≤ *N*, 1 ≤ *M* ≤ 10). Перед элементом массива с номером *K* вставить *M* новых элементов с нулевыми значениями.

## 275

Пользователь вводит массив размера *N* и два целых числа *K* и *M* (1 ≤ *K* ≤ *N*, 1 ≤ *M* ≤ 10). После элемента массива с номером *K* вставить *M* новых элементов с нулевыми значениями.

## 276

Пользователь вводит массив размера *N*. Продублировать в нем элементы с четными номерами (2, 4, …). Условный оператор в цикле не использовать.

## 277

Пользователь вводит массив размера *N*. Утроить в нем вхождения всех элементов с нечетными номерами (1, 3, …). Условный оператор в цикле не использовать.

## 278

Пользователь вводит массив размера *N*. Перед каждым положительным элементом массива вставить элемент с нулевым значением.

## 279

Пользователь вводит массив размера *N*. После каждого отрицательного элемента массива вставить элемент с нулевым значением.

## 280

Дан целочисленный массив размера *N*. Продублировать в нем все четные числа.

## 281

Дан целочисленный массив размера *N*. Утроить в нем вхождения всех нечетных чисел.

## 282

Пользователь вводит массив *A* размера *N* (≤ 6). Упорядочить его по возрастанию методом сортировки *простым обменом* (*«пузырьковой» сортировкой*): просматривать массив, сравнивая его соседние элементы (*A*<sub>1</sub> и *A*<sub>2</sub>, *A*<sub>2</sub> и *A*<sub>3</sub> и т. д.) и меняя их местами, если левый элемент пары больше правого; повторить описанные действия *N* − 1 раз. Для контроля за выполняемыми действиями выводить содержимое массива после каждого просмотра. Учесть, что при каждом просмотре количество анализируемых пар можно уменьшить на 1.

## 283

Пользователь вводит массив *A* размера *N* (≤ 6). Упорядочить его по возрастанию методом сортировки *простым выбором*: найти максимальный элемент массива и поменять его местами с последним (*N*-м) элементом; выполнить описанные действия *N* − 1 раз, каждый раз уменьшая на 1 количество анализируемых элементов и выводя содержимое массива.

## 284

Пользователь вводит массив *A* размера *N* (≤ 6). Упорядочить его по возрастанию методом сортировки *простыми вставками*: сравнить элементы *A*<sub>1</sub> и *A*<sub>2</sub> и, при необходимости меняя их местами, добиться того, чтобы они оказались упорядоченными по возрастанию; затем обратиться к элементу *A*<sub>3</sub> и переместить его в левую (уже упорядоченную) часть массива, сохранив ее упорядоченность; повторить этот процесс для остальных элементов, выводя содержимое массива после обработки каждого элемента (от 2-го до *N*-го). При выполнении описанных действий удобно использовать прием «барьера», записывая очередной элемент перед его обработкой в дополнительный элемент массива *A*<sub>0</sub>.

## 285

Пользователь вводит массив *A* размера *N*. Не изменяя данный массив, вывести номера его элементов в том порядке, в котором соответствующие им элементы образуют возрастающую последовательность. Использовать метод *«пузырьковой» сортировки*, модифицировав его следующим образом: создать вспомогательный целочисленный *массив номеров* *I*, заполнив его числами от 1 до *N*; просматривать массив *A*, сравнивая пары элементов массива *A* с номерами *I*<sub>1</sub> и *I*<sub>2</sub>, *I*<sub>2</sub> и *I*<sub>3</sub>, … и меняя местами соответствующие элементы массива *I*, если левый элемент пары больше правого. Повторив описанную процедуру просмотра *N* − 1 раз, получим в массиве *I* требуемую последовательность номеров.

## 286

Дан целочисленный массив *A* размера *N*. Назовем *серией* группу подряд идущих одинаковых элементов, а *длиной серии* — количество этих элементов (длина серии может быть равна 1). Сформировать два новых целочисленных массива *B* и *C* одинакового размера, записав в массив *B* длины всех серий исходного массива, а в массив *C* — значения элементов, образующих эти серии.

## 287

Дан целочисленный массив размера *N*. Вставить перед каждой его серией элемент с нулевым значением (определение серии дано в задании 286).

## 288

Дан целочисленный массив размера *N*. Вставить после каждой его серии элемент с нулевым значением (определение серии дано в задании 286).

## 289

Дан целочисленный массив размера *N*. Преобразовать массив, увеличив каждую его серию на один элемент (определение серии дано в задании 286).

## 290

Дан целочисленный массив размера *N*, содержащий по крайней мере одну серию, длина которой больше 1. Преобразовать массив, уменьшив каждую его серию на один элемент (определение серии дано в задании 286).

## 291

Известно целое число *K* (> 0) и целочисленный массив размера *N*. Преобразовать массив, удвоив длину его серии с номером *K* (определение серии дано в задании 286). Если серий в массиве меньше *K*, то вывести массив без изменений.

## 292

Известно целое число *K* (> 1) и целочисленный массив размера *N*. Удалить из массива серию с номером *K* (определение серии дано в задании 286). Если серий в массиве меньше *K*, то вывести массив без изменений.

## 293

Известно целое число *K* (> 1) и целочисленный массив размера *N*. Поменять местами первую серию массива и его серию с номером *K* (определение серии дано в задании 286). Если серий в массиве меньше *K*, то вывести массив без изменений.

## 294

Известно целое число *K* (> 0) и целочисленный массив размера *N*. Поменять местами последнюю серию массива и его серию с номером *K* (определение серии дано в задании 286). Если серий в массиве меньше *K*, то вывести массив без изменений.

## 295

Известно целое число *L* (> 1) и целочисленный массив размера *N*. Заменить каждую серию массива, длина которой меньше *L*, на один элемент с нулевым значением (определение серии дано в задании 286).

## 296

Известно целое число *L* (> 0) и целочисленный массив размера *N*. Заменить каждую серию массива, длина которой равна *L*, на один элемент с нулевым значением (определение серии дано в задании 286).

## 297

Известно целое число *L* (> 0) и целочисленный массив размера *N*. Заменить каждую серию массива, длина которой больше *L*, на один элемент с нулевым значением (определение серии дано в задании 286).

## 298

Дан целочисленный массив размера *N*. Преобразовать массив, увеличив его первую серию наибольшей длины на один элемент (определение серии дано в задании 286).

## 299

Дан целочисленный массив размера *N*. Преобразовать массив, увеличив его последнюю серию наибольшей длины на один элемент (определение серии дано в задании 286).

## 300

Дан целочисленный массив размера *N*. Преобразовать массив, увеличив все его серии наибольшей длины на один элемент (определение серии дано в задании 286).

## 301

Известно множество *A* из *N* точек на плоскости и точка *B* (точки заданы своими координатами *x*, *y*). Найти точку из множества *A*, наиболее близкую к точке *B*. *Расстояние* *R* между точками с координатами (*x*<sub>1</sub>, *y*<sub>1</sub>) и (*x*<sub>2</sub>, *y*<sub>2</sub>) вычисляется по формуле:

> *R* = ((*x*<sub>2</sub> − *x*<sub>1</sub>)<sup>2</sup> + (*y*<sub>2</sub> − *y*<sub>1</sub>)<sup>2</sup>)<sup>1/2</sup>.

## 302

Известно множество *A* из *N* точек (точки заданы своими координатами *x*, *y*). Среди всех точек этого множества, лежащих во второй четверти, найти точку, наиболее удаленную от начала координат. Если таких точек нет, то вывести точку с нулевыми координатами.

## 303

Известно множество *A* из *N* точек (точки заданы своими координатами *x*, *y*). Среди всех точек этого множества, лежащих в первой или третьей четверти, найти точку, наиболее близкую к началу координат. Если таких точек нет, то вывести точку с нулевыми координатами.

## 304

Известно множество *A* из *N* точек (точки заданы своими координатами *x*, *y*). Найти пару точек этого множества с максимальным расстоянием между ними и само это расстояние (точки выводятся в том же порядке, в котором они перечислены при задании множества *A*).

## 305

Известны множества *A* и *B*, состоящие соответственно из *N*<sub>1</sub> и *N*<sub>2</sub> точек (точки заданы своими координатами *x*, *y*). Найти минимальное расстояние между точками этих множеств и сами точки, расположенные на этом расстоянии (вначале выводится точка из множества *A*, затем точка из множества *B*).

## 306

Известно множество *A* из *N* точек (*N* > 2, точки заданы своими координатами *x*, *y*). Найти такую точку из данного множества, сумма расстояний от которой до остальных его точек минимальна, и саму эту сумму.

## 307

Известно множество *A* из *N* точек (*N* > 2, точки заданы своими координатами *x*, *y*). Найти наибольший периметр треугольника, вершины которого принадлежат различным точкам множества *A*, и сами эти точки (точки выводятся в том же порядке, в котором они перечислены при задании множества *A*).

## 308

Известно множество *A* из *N* точек (*N* > 2, точки заданы своими координатами *x*, *y*). Найти наименьший периметр треугольника, вершины которого принадлежат различным точкам множества *A*, и сами эти точки (точки выводятся в том же порядке, в котором они перечислены при задании множества *A*).

## 309

Известно множество *A* из *N* точек с целочисленными координатами *x*, *y*. *Порядок* на координатной плоскости определим следующим образом:

> (*x*<sub>1</sub>, *y*<sub>1</sub>) < (*x*<sub>2</sub>, *y*<sub>2</sub>), если либо *x*<sub>1</sub> < *x*<sub>2</sub>, либо *x*<sub>1</sub> = *x*<sub>2</sub> и *y*<sub>1</sub> < *y*<sub>2</sub>.

Расположить точки данного множества по возрастанию в соответствии с указанным порядком.

## 310

Известно множество *A* из *N* точек с целочисленными координатами *x*, *y*. *Порядок* на координатной плоскости определим следующим образом:

> (*x*<sub>1</sub>, *y*<sub>1</sub>) < (*x*<sub>2</sub>, *y*<sub>2</sub>), если либо *x*<sub>1</sub> + *y*<sub>1</sub> < *x*<sub>2</sub> + *y*<sub>2</sub>, либо *x*<sub>1</sub> + *y*<sub>1</sub> = *x*<sub>2</sub> + *y*<sub>2</sub> и *x*<sub>1</sub> < *x*<sub>2</sub>.

Расположить точки данного множества по убыванию в соответствии с указанным порядком.

## 311

Известны целые положительные числа *M* и *N*. Сформировать целочисленную матрицу размера *M* × *N*, у которой все элементы *I*-й строки имеют значение 10·*I* (*I* = 1, …, *M*).

## 312

Известны целые положительные числа *M* и *N*. Сформировать целочисленную матрицу размера *M* × *N*, у которой все элементы *J*-го столбца имеют значение 5·*J* (*J* = 1, …, *N*).

## 313

Известны целые положительные числа *M*, *N* и набор из *M* чисел. Сформировать матрицу размера *M* × *N*, у которой в каждом столбце содержатся все числа из исходного набора (в том же порядке).

## 314

Известны целые положительные числа *M*, *N* и набор из *N* чисел. Сформировать матрицу размера *M* × *N*, у которой в каждой строке содержатся все числа из исходного набора (в том же порядке).

## 315

Известны целые положительные числа *M*, *N*, число *D* и набор из *M* чисел. Сформировать матрицу размера *M* × *N*, у которой первый столбец совпадает с исходным набором чисел, а элементы каждого следующего столбца равны сумме соответствующего элемента предыдущего столбца и числа *D* (в результате каждая строка матрицы будет содержать элементы *арифметической прогрессии*).

## 316

Известны целые положительные числа *M*, *N*, число *D* и набор из *N* чисел. Сформировать матрицу размера *M* × *N*, у которой первая строка совпадает с исходным набором чисел, а элементы каждой следующей строки равны соответствующему элементу предыдущей строки, умноженному на *D* (в результате каждый столбец матрицы будет содержать элементы *геометрической прогрессии*).

## 317

Известна матрица размера *M* × *N* и целое число *K* (1 ≤ *K* ≤ *M*). Вывести элементы *K*-й строки данной матрицы.

## 318

Известна матрица размера *M* × *N* и целое число *K* (1 ≤ *K* ≤ *N*). Вывести элементы *K*-го столбца данной матрицы.

## 319

Известна матрица размера *M* × *N*. Вывести ее элементы, расположенные в строках с четными номерами (2, 4, …). Вывод элементов производить по строкам, условный оператор не использовать.

## 320

Известна матрица размера *M* × *N*. Вывести ее элементы, расположенные в столбцах с нечетными номерами (1, 3, …). Вывод элементов производить по столбцам, условный оператор не использовать.

## 321

Известна матрица размера *M* × *N*. Вывести ее элементы в следующем порядке: первая строка слева направо, вторая строка справа налево, третья строка слева направо, четвертая строка справа налево и т. д.

## 322

Известна матрица размера *M* × *N*. Вывести ее элементы в следующем порядке: первый столбец сверху вниз, второй столбец снизу вверх, третий столбец сверху вниз, четвертый столбец снизу вверх и т. д.

## 323

Известна квадратная матрица *A* порядка *M*. Начиная с элемента *A*<sub>1,1</sub>, вывести ее элементы следующим образом («*уголками*»): все элементы первой строки; элементы последнего столбца, кроме первого (уже выведенного) элемента; оставшиеся элементы второй строки; оставшиеся элементы предпоследнего столбца и т. д.; последним выводится элемент *A*<sub>*M*,1</sub>.

## 324

Известна квадратная матрица *A* порядка *M*. Начиная с элемента *A*<sub>1,1</sub>, вывести ее элементы следующим образом («*уголками*»): все элементы первого столбца; элементы последней строки, кроме первого (уже выведенного) элемента; оставшиеся элементы второго столбца; оставшиеся элементы предпоследней строки и т. д.; последним выводится элемент *A*<sub>1,*M*</sub>.

## 325

Известна квадратная матрица *A* порядка *M* (*M* — нечетное число). Начиная с элемента *A*<sub>1,1</sub> и перемещаясь по часовой стрелке, вывести все ее элементы *по спирали*: первая строка, последний столбец, последняя строка в обратном порядке, первый столбец в обратном порядке, оставшиеся элементы второй строки и т. д.; последним выводится центральный элемент матрицы.

## 326

Известна квадратная матрица *A* порядка *M* (*M* — нечетное число). Начиная с элемента *A*<sub>1,1</sub> и перемещаясь против часовой стрелки, вывести все ее элементы *по спирали*: первый столбец, последняя строка, последний столбец в обратном порядке, первая строка в обратном порядке, оставшиеся элементы второго столбца и т. д.; последним выводится центральный элемент матрицы.

## 327

Известна матрица размера *M* × *N* и целое число *K* (1 ≤ *K* ≤ *M*). Найти сумму и произведение элементов *K*-й строки данной матрицы.

## 328

Известна матрица размера *M* × *N* и целое число *K* (1 ≤ *K* ≤ *N*). Найти сумму и произведение элементов *K*-го столбца данной матрицы.

## 329

Известна матрица размера *M* × *N*. Для каждой строки матрицы найти сумму ее элементов.

## 330

Известна матрица размера *M* × *N*. Для каждого столбца матрицы найти произведение его элементов.

## 331

Известна матрица размера *M* × *N*. Для каждой строки матрицы с нечетным номером (1, 3, …) найти среднее арифметическое ее элементов. Условный оператор не использовать.

## 332

Известна матрица размера *M* × *N*. Для каждого столбца матрицы с четным номером (2, 4, …) найти сумму его элементов. Условный оператор не использовать.

## 333

Известна матрица размера *M* × *N*. В каждой строке матрицы найти минимальный элемент.

## 334

Известна матрица размера *M* × *N*. В каждом столбце матрицы найти максимальный элемент.

## 335

Известна матрица размера *M* × *N*. Найти номер ее строки с наибольшей суммой элементов и вывести данный номер, а также значение наибольшей суммы.

## 336

Известна матрица размера *M* × *N*. Найти номер ее столбца с наименьшим произведением элементов и вывести данный номер, а также значение наименьшего произведения.

## 337

Известна матрица размера *M* × *N*. Найти максимальный среди минимальных элементов ее строк.

## 338

Известна матрица размера *M* × *N*. Найти минимальный среди максимальных элементов ее столбцов.

## 339

Известна матрица размера *M* × *N*. В каждой ее строке найти количество элементов, меньших среднего арифметического всех элементов этой строки.

## 340

Известна матрица размера *M* × *N*. В каждом ее столбце найти количество элементов, больших среднего арифметического всех элементов этого столбца.

## 341

Известна матрица размера *M* × *N*. Найти номера строки и столбца для элемента матрицы, наиболее близкого к среднему значению всех ее элементов.

## 342

Известна целочисленная матрица размера *M* × *N*. Найти номер первой из ее строк, содержащих равное количество положительных и отрицательных элементов (нулевые элементы матрицы не учитываются). Если таких строк нет, то вывести 0.

## 343

Известна целочисленная матрица размера *M* × *N*. Найти номер последнего из ее столбцов, содержащих равное количество положительных и отрицательных элементов (нулевые элементы матрицы не учитываются). Если таких столбцов нет, то вывести 0.

## 344

Известна целочисленная матрица размера *M* × *N*. Найти номер последней из ее строк, содержащих только четные числа. Если таких строк нет, то вывести 0.

## 345

Известна целочисленная матрица размера *M* × *N*. Найти номер первого из ее столбцов, содержащих только нечетные числа. Если таких столбцов нет, то вывести 0.

## 346

Известна целочисленная матрица размера *M* × *N*, элементы которой могут принимать значения от 0 до 100. Различные строки матрицы назовем *похожими*, если совпадают множества чисел, встречающихся в этих строках. Найти количество строк, похожих на первую строку данной матрицы.

## 347

Известна целочисленная матрица размера *M* × *N*, элементы которой могут принимать значения от 0 до 100. Различные столбцы матрицы назовем *похожими*, если совпадают множества чисел, встречающихся в этих столбцах. Найти количество столбцов, похожих на последний столбец данной матрицы.

## 348

Известна целочисленная матрица размера *M* × *N*. Найти количество ее строк, все элементы которых различны.

## 349

Известна целочисленная матрица размера *M* × *N*. Найти количество ее столбцов, все элементы которых различны.

## 350

Известна целочисленная матрица размера *M* × *N*. Найти номер последней из ее строк, содержащих максимальное количество одинаковых элементов.

## 351

Известна целочисленная матрица размера *M* × *N*. Найти номер первого из ее столбцов, содержащих максимальное количество одинаковых элементов.

## 352

Известна матрица размера *M* × *N*. Найти количество ее строк, элементы которых упорядочены по возрастанию.

## 353

Известна матрица размера *M* × *N*. Найти количество ее столбцов, элементы которых упорядочены по убыванию.

## 354

Известна матрица размера *M* × *N*. Найти минимальный среди элементов тех строк, которые упорядочены либо по возрастанию, либо по убыванию. Если упорядоченные строки в матрице отсутствуют, то вывести 0.

## 355

Известна матрица размера *M* × *N*. Найти максимальный среди элементов тех столбцов, которые упорядочены либо по возрастанию, либо по убыванию. Если упорядоченные столбцы в матрице отсутствуют, то вывести 0.

## 356

Известна целочисленная матрица размера *M* × *N*. Найти элемент, являющийся максимальным в своей строке и минимальным в своем столбце. Если такой элемент отсутствует, то вывести 0.

## 357

Дан символ *C*. Вывести его *код* (т. е. номер в кодовой таблице).

## 358

Известно целое число *N* (32 ≤ *N* ≤ 126). Вывести символ с кодом, равным *N*.

## 359

Дан символ *C*. Вывести два символа, первый из которых предшествует символу *C* в кодовой таблице, а второй следует за символом *C*.

## 360

Известно целое число *N* (1 ≤ *N* ≤ 26). Вывести *N* первых *прописных* (т. е. заглавных) букв латинского алфавита.

## 361

Известно целое число *N* (1 ≤ *N* ≤ 26). Вывести *N* последних *строчных* (т. е. маленьких) букв латинского алфавита в обратном порядке (начиная с буквы «z»).

## 362

Дан символ *C*, изображающий цифру или букву (латинскую или русскую). Если *C* изображает цифру, то вывести строку «digit», если латинскую букву — вывести строку «lat», если русскую — вывести строку «rus».

## 363

Известна непустая строка. Вывести коды ее первого и последнего символа.

## 364

Известно целое число *N* (> 0) и символ *C*. Вывести строку длины *N*, которая состоит из символов *C*.

## 365

Известно четное число *N* (> 0) и символы *C*<sub>1</sub> и *C*<sub>2</sub>. Вывести строку длины *N*, которая состоит из чередующихся символов *C*<sub>1</sub> и *C*<sub>2</sub>, начиная с *C*<sub>1</sub>.

## 366

Известна строка. Вывести строку, содержащую те же символы, но расположенные в обратном порядке.

## 367

Известна непустая строка *S*. Вывести строку, содержащую символы строки *S*, между которыми вставлено по три пробела.

## 368

Известна непустая строка *S* и целое число *N* (> 0). Вывести строку, содержащую символы строки *S*, между которыми вставлено по *N* символов «*» (звездочка).

## 369

Известна строка. Подсчитать количество содержащихся в ней цифр.

## 370

Известна строка. Подсчитать количество содержащихся в ней прописных латинских букв.

## 371

Известна строка. Подсчитать общее количество содержащихся в ней строчных латинских и русских букв.

## 372

Известна строка. Преобразовать в ней все прописные латинские буквы в строчные.

## 373

Известна строка. Преобразовать в ней все строчные буквы (как латинские, так и русские) в прописные.

## 374

Известна строка. Преобразовать в ней все строчные буквы (как латинские, так и русские) в прописные, а прописные — в строчные.

## 375

Известна строка. Если она представляет собой запись целого числа, то вывести 1, если вещественного (с дробной частью) — вывести 2; если строку нельзя преобразовать в число, то вывести 0. Считать, что дробная часть вещественного числа отделяется от его целой части десятичной *точкой* «.».

## 376

Известно целое положительное число. Вывести символы, изображающие цифры этого числа (в порядке слева направо).

## 377

Известно целое положительное число. Вывести символы, изображающие цифры этого числа (в порядке справа налево).

## 378

Известна строка, изображающая целое положительное число. Вывести сумму цифр этого числа.

## 379

Известна строка, изображающая арифметическое выражение вида «<цифра>±<цифра>±…±<цифра>», где на месте знака операции «±» находится символ «+» или «−» (например, «4+7−2−8»). Вывести значение данного выражения (целое число).

## 380

Известна строка, изображающая двоичную запись целого положительного числа. Вывести строку, изображающую десятичную запись этого же числа.

## 381

Известна строка, изображающая десятичную запись целого положительного числа. Вывести строку, изображающую двоичную запись этого же числа.

## 382

Известно целое число *N* (> 0) и строка *S*. Преобразовать строку *S* в строку длины *N* следующим образом: если длина строки *S* больше *N*, то отбросить первые символы, если длина строки *S* меньше *N*, то в ее начало добавить символы «.» (точка).

## 383

Известны целые положительные числа *N*<sub>1</sub> и *N*<sub>2</sub> и строки *S*<sub>1</sub> и *S*<sub>2</sub>. Получить из этих строк новую строку, содержащую первые *N*<sub>1</sub> символов строки *S*<sub>1</sub> и последние *N*<sub>2</sub> символов строки *S*<sub>2</sub> (в указанном порядке).

## 384

Дан символ *C* и строка *S*. Удвоить каждое вхождение символа *C* в строку *S*.

## 385

Дан символ *C* и строки *S*, *S*<sub>0</sub>. Перед каждым вхождением символа *C* в строку *S* вставить строку *S*<sub>0</sub>.

## 386

Дан символ *C* и строки *S*, *S*<sub>0</sub>. После каждого вхождения символа *C* в строку *S* вставить строку *S*<sub>0</sub>.

## 387

Известны строки *S* и *S*<sub>0</sub>. Проверить, содержится ли строка *S*<sub>0</sub> в строке *S*. Если содержится, то вывести True, если не содержится, то вывести False.

## 388

Известны строки *S* и *S*<sub>0</sub>. Найти количество вхождений строки *S*<sub>0</sub> в строку *S*.

## 389

Известны строки *S* и *S*<sub>0</sub>. Удалить из строки *S* первую подстроку, совпадающую с *S*<sub>0</sub>. Если совпадающих подстрок нет, то вывести строку *S* без изменений.

## 390

Известны строки *S* и *S*<sub>0</sub>. Удалить из строки *S* последнюю подстроку, совпадающую с *S*<sub>0</sub>. Если совпадающих подстрок нет, то вывести строку *S* без изменений.

## 391

Известны строки *S* и *S*<sub>0</sub>. Удалить из строки *S* все подстроки, совпадающие с *S*<sub>0</sub>. Если совпадающих подстрок нет, то вывести строку *S* без изменений.

## 392

Известны строки *S*, *S*<sub>1</sub> и *S*<sub>2</sub>. Заменить в строке *S* первое вхождение строки *S*<sub>1</sub> на строку *S*<sub>2</sub>.

## 393

Известны строки *S*, *S*<sub>1</sub> и *S*<sub>2</sub>. Заменить в строке *S* последнее вхождение строки *S*<sub>1</sub> на строку *S*<sub>2</sub>.

## 394

Известны строки *S*, *S*<sub>1</sub> и *S*<sub>2</sub>. Заменить в строке *S* все вхождения строки *S*<sub>1</sub> на строку *S*<sub>2</sub>.

## 395

Известна строка, содержащая по крайней мере один символ пробела. Вывести подстроку, расположенную между первым и вторым пробелом исходной строки. Если строка содержит только один пробел, то вывести пустую строку.

## 396

Известна строка, содержащая по крайней мере один символ пробела. Вывести подстроку, расположенную между первым и последним пробелом исходной строки. Если строка содержит только один пробел, то вывести пустую строку.

## 397

Известна строка, состоящая из русских слов, разделенных пробелами (одним или несколькими). Найти количество слов в строке.

## 398

Известна строка, состоящая из русских слов, набранных заглавными буквами и разделенных пробелами (одним или несколькими). Найти количество слов, которые начинаются и заканчиваются одной и той же буквой.

## 399

Известна строка, состоящая из русских слов, набранных заглавными буквами и разделенных пробелами (одним или несколькими). Найти количество слов, которые содержат хотя бы одну букву «А».

## 400

Известна строка, состоящая из русских слов, набранных заглавными буквами и разделенных пробелами (одним или несколькими). Найти количество слов, которые содержат ровно три буквы «А».

## 401

Известна строка, состоящая из русских слов, разделенных пробелами (одним или несколькими). Найти длину самого короткого слова.

## 402

Известна строка, состоящая из русских слов, разделенных пробелами (одним или несколькими). Найти длину самого длинного слова.

## 403

Известна строка, состоящая из русских слов, разделенных пробелами (одним или несколькими). Вывести строку, содержащую эти же слова, разделенные одним символом «.» (точка). В конце строки точку не ставить.

## 404

Известна строка, состоящая из русских слов, набранных заглавными буквами и разделенных пробелами (одним или несколькими). Преобразовать каждое слово в строке, заменив в нем все последующие вхождения его первой буквы на символ «.» (точка). Например, слово «МИНИМУМ» надо преобразовать в «МИНИ.У.». Количество пробелов между словами не изменять.

## 405

Известна строка, состоящая из русских слов, набранных заглавными буквами и разделенных пробелами (одним или несколькими). Преобразовать каждое слово в строке, заменив в нем все предыдущие вхождения его последней буквы на символ «.» (точка). Например, слово «МИНИМУМ» надо преобразовать в «.ИНИ.УМ». Количество пробелов между словами не изменять.

## 406

Известна строка, состоящая из русских слов, разделенных пробелами (одним или несколькими). Вывести строку, содержащую эти же слова, разделенные одним пробелом и расположенные в обратном порядке.

## 407

Известна строка, состоящая из русских слов, набранных заглавными буквами и разделенных пробелами (одним или несколькими). Вывести строку, содержащую эти же слова, разделенные одним пробелом и расположенные в алфавитном порядке.

## 408

Известна строка-предложение на русском языке. Преобразовать строку так, чтобы каждое слово начиналось с заглавной буквы. *Словом* считать набор символов, не содержащий пробелов и ограниченный пробелами или началом/концом строки. Слова, не начинающиеся с буквы, не изменять.

## 409

Известна строка-предложение на русском языке. Подсчитать количество содержащихся в строке знаков препинания.

## 410

Известна строка-предложение на русском языке. Подсчитать количество содержащихся в строке гласных букв.

## 411

Известна строка-предложение на русском языке. Вывести самое длинное слово в предложении. Если таких слов несколько, то вывести первое из них. *Словом* считать набор символов, не содержащий пробелов, знаков препинания и ограниченный пробелами, знаками препинания или началом/концом строки.

## 412

Известна строка-предложение на русском языке. Вывести самое короткое слово в предложении. Если таких слов несколько, то вывести последнее из них. *Словом* считать набор символов, не содержащий пробелов, знаков препинания и ограниченный пробелами, знаками препинания или началом/концом строки.

## 413

Известна строка-предложение с избыточными пробелами между словами. Преобразовать ее так, чтобы между словами был ровно один пробел.

## 414

Известно имя файла и целые положительные числа *N* и *K*. Создать текстовый файл с указанным именем и записать в него *N* строк, каждая из которых состоит из *K* символов «*» (звездочка).

## 415

Известно имя файла и целое число *N* (0 < *N* < 27). Создать текстовый файл с указанным именем и записать в него *N* строк: первая строка должна содержать *строчную* (т. е. маленькую) латинскую букву «a», вторая — буквы «ab», третья — буквы «abc» и т. д.; последняя строка должна содержать *N* начальных строчных латинских букв в алфавитном порядке.

## 416

Известно имя файла и целое число *N* (0 < *N* < 27). Создать текстовый файл с указанным именем и записать в него *N* строк длины *N*; строка с номером *K* (*K* = 1, …, *N*) должна содержать *K* начальных *прописных* (т. е. заглавных) латинских букв, дополненных справа символами «*» (звездочка). Например, для *N* = 4 файл должен содержать строки «A***», «AB**», «ABC*», «ABCD».

## 417

Дан текстовый файл. Вывести количество содержащихся в нем символов и строк (маркеры концов строк EOLN и конца файла EOF при подсчете количества символов не учитывать).

## 418

Известна строка *S* и текстовый файл. Добавить строку *S* в конец файла.

## 419

Известны два текстовых файла. Добавить в конец первого файла содержимое второго файла.

## 420

Известна строка *S* и текстовый файл. Добавить строку *S* в начало файла.

## 421

Известны два текстовых файла. Добавить в начало первого файла содержимое второго файла.

## 422

Известно целое число *K* и текстовый файл. Вставить пустую строку перед строкой файла с номером *K*. Если строки с таким номером нет, то оставить файл без изменений.

## 423

Известно целое число *K* и текстовый файл. Вставить пустую строку после строки файла с номером *K*. Если строки с таким номером нет, то оставить файл без изменений.

## 424

Дан текстовый файл. Продублировать в нем все пустые строки.

## 425

Известна строка *S* и текстовый файл. Заменить в файле все пустые строки на строку *S*.

## 426

Дан непустой текстовый файл. Удалить из него первую строку.

## 427

Дан непустой текстовый файл. Удалить из него последнюю строку.

## 428

Известно целое число *K* и текстовый файл. Удалить из файла строку с номером *K*. Если строки с таким номером нет, то оставить файл без изменений.

## 429

Дан текстовый файл. Удалить из него все пустые строки.

## 430

Известны два текстовых файла. Добавить в конец каждой строки первого файла соответствующую строку второго файла. Если второй файл короче первого, то оставшиеся строки первого файла не изменять.

## 431

Известно целое число *K* и текстовый файл. Удалить из каждой строки файла первые *K* символов (если длина строки меньше *K*, то удалить из нее все символы).

## 432

Дан текстовый файл. Заменить в нем все прописные русские буквы на строчные, а все строчные — на прописные.

## 433

Дан текстовый файл. Заменить в нем все подряд идущие пробелы на один пробел.

## 434

Дан текстовый файл, содержащий более трех строк. Удалить из него последние три строки.

## 435

Известно целое число *K* (0 < *K* < 10) и текстовый файл, содержащий более *K* строк. Удалить из файла последние *K* строк.

## 436

Известно целое число *K* (0 < *K* < 10) и текстовый файл, содержащий более *K* строк. Создать новый текстовый файл, содержащий *K* последних строк исходного файла.
